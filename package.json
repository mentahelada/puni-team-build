{
  "name": "mi-web-estatica",
  "version": "1.0.0",
  "description": "Web estática (HTML/CSS/JS) desplegada en Render",
  "private": true,
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000}",
    "build": "echo \"No hace falta compilar: es una web estática\""
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
